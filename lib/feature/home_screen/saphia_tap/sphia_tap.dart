import 'package:flutter/material.dart';

class SphiaTap extends StatelessWidget {
  const SphiaTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
    );
  }
}
